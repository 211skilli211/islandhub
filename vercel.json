{
  "buildCommand": "npm run build",
  "outputDirectory": ".next",
  "installCommand": "npm install",
  "framework": "nextjs",
  "projectDir": "web",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "https://islandhub.onrender.com/api/$1"
    }
  ]
}
