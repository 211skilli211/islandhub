import { Agent, Rule, Workflow, Skill } from '@antigravity/core';

class IslandFundAgent extends Agent {
  constructor() {
    super();
    console.log('🌴 IslandFund Agent initialized');
  }
}

export default IslandFundAgent;