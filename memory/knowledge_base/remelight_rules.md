# ReMeLight Rules

The ReMeLight framework strictly enforces structured context memory using root directory `/memory`.
Dense data is stored in a SQLite database called `technical_data.db`.
Vector embeddings are retrieved with an explicit 0.7 Vector and 0.3 BM25 blend score.
